#include<iostream>
using namespace std;

class Vigenere_Cipher
{
    public:

      Vigenere_Cipher();
      string Encryption(string Ans,string key);
      string Decryption(string Ans,string key);
};